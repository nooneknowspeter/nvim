return {
	ensure_installed = { "lua", "luadoc", "printf", "vim", "vimdoc", "markdown", "html" },

	auto_install = true,

	highlight = {
		enable = true,
		use_languagetree = true,
	},

	indent = { enable = true },
}
